# InviteHub legacy backend

This folder is a disabled legacy Express backend stub. The supported runtime for
the product is the Next.js `app/api` layer backed by Supabase.

The stub remains syntactically valid so repository-wide checks do not fail on
dead code, but `/api/*` requests intentionally return `410 Gone`.
